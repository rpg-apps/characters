export default class Context {
  constructor (props) {
    Object.assign(this, props)
  }

  getValue (valuble, character) {
  	// Type is irrelevant, since at this stage all of their values are values, and the Types are just references
  	// Choice.getValue(character)
  	// Field.getValue(character)
  	// Field.getValue(character, params=[], this.getValue)
    // FORMULA parser?
  }
}