import Move from '../../../move'
import Procedure, { modifier, CONSTANT } from '../../../move_procedure'

const wellTrained = new Move({
	title: 'Well Trained',
	text: 
`Choose another training for your animal companion.`,

	procedure: new Procedure(CONSTANT, )
})

export default wellTrained