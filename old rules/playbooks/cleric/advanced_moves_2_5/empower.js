import Move from '../../../move'
import Procedure, { choice, condition, CONSTANT } from '../../../move_procedure'

const empower = new Move({
	title: 'Empower',
	text: 
``,

	procedure: new Procedure(CONSTANT, )
})

export default empower