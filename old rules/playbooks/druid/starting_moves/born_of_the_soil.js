import Move from '../../../move'
import Procedure, { CONSTANT } from '../../../move_procedure'

const bornOfTheSoil = new Move({
	title: 'Born Of The Soil',
	text: 
``,

	procedure: new Procedure(CONSTANT, )
})

export default bornOfTheSoil