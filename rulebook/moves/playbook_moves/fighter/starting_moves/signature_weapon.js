import Move, { Procedure } from '../move'

const {  } = Procedure

const signatureWeapon = new Move({
	title: 'Signature Weapon',
	text: 
``,

	procedure: new Procedure('', )
})

export default signatureWeapon