import Move, { Procedure } from '../move'

const { modifier, CONSTANT } = Procedure

const ironHide = new Move({
	title: 'Iron Hide',
	text: 
`You gain +1 armor.`,

	procedure: new Procedure(CONSTANT, )
})

export default ironHide