import Move, { Procedure } from '../move'

const { choice, condition, CONSTANT } = Procedure

const empower = new Move({
	title: 'Empower',
	text: 
``,

	procedure: new Procedure(CONSTANT, )
})

export default empower