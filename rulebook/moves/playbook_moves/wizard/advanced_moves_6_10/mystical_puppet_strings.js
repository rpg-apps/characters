import Move, { Procedure } from '../move'

const {  } = Procedure

const mysticalPuppetStrings = new Move({
	title: 'Mystical Puppet Strings',
	text: 
`When you use magic to control a person’s actions they have no memory of what you had them do and bear you no ill will.`,

	procedure: new Procedure('When you use magic to control a person’s actions', they have no memory of what you had them do and bear you no ill will.)
})

export default mysticalPuppetStrings