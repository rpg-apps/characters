import Move, { Procedure } from '../move'

const {  } = Procedure

const burnsHalfAsLong = new Move({
	title: 'Burns Half As Long',
	text: 
`You gain this move when you gain Burns Twice as Bright.`,

	procedure: new Procedure('', )
})

export default burnsHalfAsLong