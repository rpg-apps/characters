import Move from '../../../move'
import Procedure from '../../../move_procedure'

const quest = new Move({
	title: 'Quest',
	text: 
`When you dedicate yourself to a mission through prayer and ritual cleansing`,

	procedure: new Procedure('When you dedicate yourself to a mission through prayer and ritual cleansing', )
})

export default quest